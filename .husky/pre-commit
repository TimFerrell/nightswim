echo "🔍 Running pre-commit checks..."

# Run lint-staged to check and fix linting issues
npx lint-staged

# Run tests on staged files (optional - can be slow)
# echo "🧪 Running tests on staged files..."
# npm run test:ci

echo "✅ Pre-commit checks completed!"
